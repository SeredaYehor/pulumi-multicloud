name: pulumi-multicloud
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: Project for deploying multicloud infrastructure based on AWS and Azure
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
